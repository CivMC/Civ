name: ${project.name}
main: com.github.maxopoly.KiraBukkitGateway.KiraBukkitGatewayPlugin
author: Maxopoly
version: ${project.version}
depend: [Vault, CivModCore, NameLayer, CivChat2]
softdepend: []
commands:
permissions:
api-version: 1.12.2
commands:
    discordauth:
        permission: kira.public
    linkdiscordchannel:
        permission: kira.public
    deletediscordchannel:
        permission: kira.public
    syncdiscordchannel:
        permission: kira.public
    kirareload:
        permission: kira.op
permissions:
    kira.public:
        default: true
    kira.op:
        default: op       