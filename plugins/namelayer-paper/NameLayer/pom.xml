<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                      http://maven.apache.org/xsd/maven-4.0.0.xsd">
<modelVersion>4.0.0</modelVersion>
	<groupId>vg.civcraft.mc.namelayer</groupId>
	<artifactId>NameLayer</artifactId>
	<packaging>jar</packaging>
	<version>2.7.02</version>
	<name>NameLayer</name>
	<url>https://github.com/Civcraft/NameLayer/</url>			  
	
	<dependencies>
		<dependency>
			<groupId>vg.civcraft.mc.namelayer</groupId>
			<artifactId>NameLayerMain</artifactId>
			<version>1.0.0</version>
		</dependency>
	</dependencies>
	
	<build>
		<defaultGoal>clean package</defaultGoal>
		<directory>../target</directory> <!-- Build it one level up, because it is the one we care about -->
		<finalName>NameLayer-${project.version}</finalName>
		<resources>
			<resource>
				<targetPath>.</targetPath>
				<filtering>true</filtering>
				<directory>..</directory>
				<includes>
					<include>LICENSE.txt</include>
				</includes>
			</resource>
			<!-- plugin.yml here to fill in the correct content. -->
			<resource>
				<targetPath>.</targetPath>
				<filtering>true</filtering>
				<directory>../NameLayerMain</directory>
				<includes>
					<include>plugin.yml</include>
				</includes>
			</resource>
			<resource>
				<targetPath>.</targetPath>
				<filtering>true</filtering>
				<directory>../NameLayerBungee</directory>
				<includes>
					<include>bungee.yml</include>
				</includes>
			</resource>
			<!-- config.yml needs its own location so it does not mess with the bukkit plugin. -->
			<resource>
				<targetPath>./bungee</targetPath>
				<filtering>true</filtering>
				<directory>../NameLayerBungee</directory>
				<includes>
					<include>config.yml</include>
				</includes>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>2.5.1</version>
				<configuration>
					<source>1.6</source>
					<target>1.6</target>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<version>2.4.3</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
						<configuration>
							<artifactSet>
								<includes>
									<include>vg.civcraft.mc.namelayer:*</include>
								</includes>
							</artifactSet>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
				<configuration>
					<!-- <skip>true</skip> -->
				</configuration>
			</plugin>
		</plugins>
	</build>
	
</project>
