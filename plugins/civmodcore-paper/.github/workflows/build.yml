name: Build

on:
  push:
    branches:
      - '*'
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
   uses: civmc/.github/.github/workflows/gradle_build@master
   with:
     gradle_task: reobf
   secrets: inherit
