name: ${project.name}
version: ${project.version}
authors: [valadian, rourke750, ProgrammerDan, Maxopoly, DeadBeef, Protonull]
main: ${project.groupId}.${project.name}Plugin
api-version: 1.13
dev-url: null
commands:
  config:
    description: Allows configuring player specific settings
    usage: /config
