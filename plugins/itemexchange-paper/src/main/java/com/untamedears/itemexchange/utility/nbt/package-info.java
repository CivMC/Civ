/**
 * This package was moved from CivModCore to ItemExchange because 1) it's the only plugin that uses it, and 2) to avoid
 * any other plugin from potentially using it. It's bad and needs to be removed. But given that that would require a
 * substantial refactor to ItemExchange, localising this package to ItemExchange seems like a reasonable compromise.
 */
package com.untamedears.itemexchange.utility.nbt;
