name: ${project.name}
main: com.github.civcraft.donum.Donum
author: Maxopoly
version: ${project.version}
depend: [CivModCore, NameLayer]
commands:
  present:
  deliver:
    permission: donum.op
  deliverdeath:
    permission: donum.op
permissions:
  donum.op:
    default: op
