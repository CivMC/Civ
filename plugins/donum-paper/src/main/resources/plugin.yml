name: ${project.name}
main: com.github.civcraft.donum.Donum
author: Maxopoly
version: ${project.version}
depend: [CivModCore, NameLayer]
api-version: 1.16
commands:
  present:
  deliver:
    permission: donum.op
  deliverdeath:
    permission: donum.op
permissions:
  donum.op:
    default: op
